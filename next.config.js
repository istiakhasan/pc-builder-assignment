/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.gamingscan.com','https://media.istockphoto.com'],
  },
}

module.exports = nextConfig
